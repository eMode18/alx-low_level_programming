#include <stdio.h>
/**
 * main - results in an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("infinite loop incoming :(\n");

	i = 0;
	/**
	 * while (i < 10)
	 * {
	 * 	putchar(i);
	 * }
	 */
	printf("infinite loop avaoided! \\o/\n");

	return (0);
}
