#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H
/**
 * ABS - defining a macro like function - ABS(x) that
 * calculates the absolute value of int x
 * @x: - int
 */
#define ABS(x) ((x) < (0) ? -(x) : (x))

#endif

