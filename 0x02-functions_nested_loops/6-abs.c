#include "main.h"

/**
 * _abs - calculates the value absolute value of an integer
 * @n: the int value to check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
