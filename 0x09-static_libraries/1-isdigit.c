#include "main.h"
/**
 * _isdigit - evaluates if c is a degit; range 0-9
 * @c: int to evaluate
 * Return: If c is a digit, return 1, else return 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

