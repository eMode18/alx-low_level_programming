#include "main.h"
/**
 * _islower - evaluates whether c is a lowercase character
 * @c: character to evaluate
 * Return: 1 if c is a lowercase character, else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

